## Deploy a basic web server on an EC2 instance:

1. Launch an Ubuntu EC2 instance in the public subnet of your VPC.
2. Assign a security group that allows SSH (port 22) and HTTP (port 80).
3. Install Apache Web Server and deploy a basic HTML page.